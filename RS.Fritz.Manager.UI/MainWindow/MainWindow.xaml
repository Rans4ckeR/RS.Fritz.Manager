<RibbonWindow x:Class="RS.Fritz.Manager.UI.MainWindow"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="clr-namespace:RS.Fritz.Manager.UI"
        xmlns:domain="clr-namespace:RS.Fritz.Manager.Domain;assembly=RS.Fritz.Manager.Domain"
        d:DataContext="{d:DesignInstance Type=ui:MainWindowViewModel}"
        mc:Ignorable="d"
        Title="{Binding Title}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Ribbon Grid.Row="0">
            <Ribbon.ApplicationMenu>
                <RibbonApplicationMenu Visibility="Hidden" />
            </Ribbon.ApplicationMenu>
            <Ribbon.ContextMenu>
                <ContextMenu Visibility="Hidden"/>
            </Ribbon.ContextMenu>
            <RibbonTab Header="Discovery &amp; Login" IsEnabled="{Binding DeviceAndLoginControlsEnabled}">
                <RibbonGroup Header="Device Discovery">
                    <RibbonButton Label="Discover Internet Gateway Devices" LargeImageSource="/RS.Fritz.Manager.UI;component/Images/Search.png" Command="{Binding DiscoverDevicesCommand}"/>
                </RibbonGroup>
                <RibbonGroup Header="Device Selection">
                    <RibbonComboBox Label="Select A Discovered Device:" LargeImageSource="/RS.Fritz.Manager.UI;component/Images/Device.png">
                        <RibbonGallery SelectedItem="{Binding SelectedDevice}">
                            <RibbonGalleryCategory ItemsSource="{Binding Devices}">
                                <RibbonGalleryCategory.ItemTemplate>
                                    <DataTemplate DataType="{x:Type domain:InternetGatewayDevice}">
                                        <TextBlock Text="{Binding Server}" />
                                    </DataTemplate>
                                </RibbonGalleryCategory.ItemTemplate>
                            </RibbonGalleryCategory>
                        </RibbonGallery>
                    </RibbonComboBox>
                </RibbonGroup>
                <RibbonGroup Header="User Selection">
                    <RibbonComboBox Label="Select A User:" LargeImageSource="/RS.Fritz.Manager.UI;component/Images/User.png">
                        <RibbonGallery SelectedItem="{Binding SelectedUser}">
                            <RibbonGalleryCategory ItemsSource="{Binding Users}">
                                <RibbonGalleryCategory.ItemTemplate>
                                    <DataTemplate DataType="{x:Type domain:User}">
                                        <TextBlock Text="{Binding Name}" />
                                    </DataTemplate>
                                </RibbonGalleryCategory.ItemTemplate>
                            </RibbonGalleryCategory>
                        </RibbonGallery>
                    </RibbonComboBox>
                </RibbonGroup>
                <RibbonGroup Header="User Login">
                    <RibbonTextBox Label="Enter User Password:" Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" LargeImageSource="/RS.Fritz.Manager.UI;component/Images/Password.png"/>
                </RibbonGroup>
            </RibbonTab>
            <RibbonTab Header="Device information">
                <RibbonGroup Header="Get DeviceInfo" ToolTip="This service allows the reporting of basic device information, including the current hardware and software versions.">
                    <RibbonButton Label="DeviceInfo" Command="{Binding DeviceInfoViewModel.GetDeviceInfoCommand}" LargeImageSource="/RS.Fritz.Manager.UI;component/Images/Search.png" />
                </RibbonGroup>
                <RibbonGroup Header="Get LanConfigSecurity" ToolTip="This service enables the ability to set the password that will allow the device to be configured via the xml interface.">
                    <RibbonButton Label="LanConfigSecurity" Command="{Binding LanConfigSecurityViewModel.GetLanConfigSecurityCommand}" LargeImageSource="/RS.Fritz.Manager.UI;component/Images/Search.png" />
                </RibbonGroup>
                <RibbonGroup Header="Get WanDslInterfaceConfig" ToolTip="The WANDSLInterfaceConfig service models physical layer properties specific to a single physical connection of a DSL modem used for Internet access on an IGD.">
                    <RibbonButton Label="WanDslInterfaceConfig" Command="{Binding WanDslInterfaceConfigViewModel.GetWanDslInterfaceConfigCommand}" LargeImageSource="/RS.Fritz.Manager.UI;component/Images/Search.png" />
                </RibbonGroup>
                <RibbonGroup Header="Set LanConfigSecurity" ToolTip="Replaces the existing ConfigPassword with the value specified in NewPassword.">
                    <RibbonButton Label="Update Password" Command="{Binding LanConfigSecurityViewModel.SetConfigPasswordCommand}" LargeImageSource="/RS.Fritz.Manager.UI;component/Images/Search.png" />
                    <RibbonTextBox Label="New Password:" Text="{Binding LanConfigSecurityViewModel.LanConfigSecuritySetConfigPasswordValue, UpdateSourceTrigger=PropertyChanged}"/>
                    <Label Content="Changing the password needs up to 20 seconds."/>
                </RibbonGroup>
            </RibbonTab>
        </Ribbon>

        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Row="0" Text="InternetGatewayDevice:"/>
                <ContentControl Grid.Row="1" Content="{Binding DeviceLoginInfo.Device}" />
            </Grid>
        </ScrollViewer>

        <ScrollViewer Grid.Row="2" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <ContentControl Content="{Binding ActiveView}"/>
        </ScrollViewer>
        <ScrollViewer Grid.Row="3" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <TextBlock Text="{Binding UserMessage}"/>
        </ScrollViewer>
    </Grid>
</RibbonWindow>